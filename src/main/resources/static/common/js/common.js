const commonLib = {
    /**
     *
     * @param url
     * @param method
     * @param data
     * @param headers
     */
    ajaxLoad(url, method, data, headers) {
        if (!url) {
            return;
        }
    }
};